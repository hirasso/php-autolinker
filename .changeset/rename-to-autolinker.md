---
"php-autolinker": minor
---

Rename the package to `hirasso/autolinker`. The namespace is now `Hirasso\Autolinker` and the main class is `Autolinker` (was `Autolink`). Update your imports accordingly; the `autolink()` helper function keeps its name.
